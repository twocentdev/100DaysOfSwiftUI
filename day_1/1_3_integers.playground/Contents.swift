let baseScore = 0
let goalScore = 1

var score = baseScore
print(score)
score += goalScore
print(score)

let million = 1_000_000 // "_" is used to print separators into numbers.
print(million)
print(million + 1)

print(million.isMultiple(of: 6)) //Like with strings, you can apply functions to integer variables.
